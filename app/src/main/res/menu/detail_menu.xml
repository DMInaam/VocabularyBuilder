<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!--
    This menu item is used for actions like deleting the current word.
    It uses our reusable ic_delete icon and is set to show in the Toolbar if there is room.
    -->
    <item
        android:id="@+id/action_delete_word"
        android:icon="@drawable/ic_delete"
        android:title="Delete"
        app:showAsAction="ifRoom"
        app:iconTint="?attr/colorError" /> <!-- IMPROVEMENT: Added tint -->
</menu>