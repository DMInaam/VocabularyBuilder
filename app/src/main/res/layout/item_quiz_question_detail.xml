<?xml version="1.0" encoding="utf-8"?>
<!--
IMPROVEMENT 1: Switched to ConstraintLayout for a flat, efficient layout
that allows for easy alignment of text and icons.
-->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp">

    <!--
    IMPROVEMENT 2: Added an icon to visually show
    if the answer was correct or incorrect.
    -->
    <ImageView
        android:id="@+id/answer_status_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginEnd="8dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/question_text_view"
        app:tint="?attr/colorError"
        tools:src="@drawable/ic_cancel"
        android:contentDescription="Answer status icon" />

    <TextView
        android:id="@+id/question_text_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:textAppearance="?attr/textAppearanceBody1"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/answer_status_icon"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="What is the definition of 'Ephemeral'?" />

    <TextView
        android:id="@+id/user_answer_text_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textStyle="italic"
        android:textAppearance="?attr/textAppearanceBody2"
        app:layout_constraintEnd_toEndOf="@id/question_text_view"
        app:layout_constraintStart_toStartOf="@id/question_text_view"
        app:layout_constraintTop_toBottomOf="@id/question_text_view"
        tools:text="Your answer: Lasting for a long time" />

    <TextView
        android:id="@+id/correct_answer_text_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:textAppearance="?attr/textAppearanceBody2"
        android:textColor="?attr/colorOnSurfaceVariant"
        app:layout_constraintEnd_toEndOf="@id/question_text_view"
        app:layout_constraintStart_toStartOf="@id/question_text_view"
        app:layout_constraintTop_toBottomOf="@id/user_answer_text_view"
        tools:text="Correct: Lasting for a very short time" />

</androidx.constraintlayout.widget.ConstraintLayout>